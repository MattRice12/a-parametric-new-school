<h1><%= teacher.school.name %></h1>

<h1><%= teacher.name %></h1>

<button type="edit" class="button-primary edit-teacher"><%= link_to("Edit", edit_teacher_path) %></button>

<button type="delete" class="cutton-primary delete-teacher"><%= link_to "Delete", teacher_path, method: :delete, data: {confirm: "Are you sure?"} %></button>

  <div class="teacher-student-block">

    <% teacher.students.order(:name).each do |student| %>

    <h3 class="student-name"> <%= link_to student.name, student %>

    <% end %>
  </div>
