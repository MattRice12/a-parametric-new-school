<h2> <%= form_title %> </h2>

<%= form_for(student) do |f| %>
  <fieldset>
    <%= f.text_area :name, placeholder: 'Mike Blandersonman' %>
  </fieldset>

  <div class='actions text-center'>
    <%= f.submit class: 'button-primary' %>
  </div>
<% end %>
