
<div><%= link_to school.name, school_path(school) %>
<%= teacher.name %>

  <h3 class="student-name"> <%= student.name %></h3>

  <button type="edit" class="button-primary edit-student"><%= link_to("Edit", edit_student_path) %></button>

  <button type="delete" class="cutton-primary delete-student"><%= link_to "Delete", student_path, method: :delete, data: {confirm: "Are you sure?"} %></button>
