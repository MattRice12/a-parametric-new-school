<h2> <%= form_title %> </h2>

<%= form_for(school) do |f| %>
  <fieldset>
    <%= f.text_area :name, placeholder: 'Grenedine HighSchool' %>
  </fieldset>

  <div class='actions text-center'>
    <%= f.submit class: 'button-primary' %>
  </div>
<% end %>
